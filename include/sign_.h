#ifndef PROYECTOTALLER_SIGN__H
#define PROYECTOTALLER_SIGN__H


/**
 * @brief Devuelve el valor absoluto de `a` con el signo de `b`.
 * @param a Valor del cual se toma el valor absoluto.
 * @param b Valor cuyo signo se aplica.
 * @return Valor absoluto de `a` con el signo de `b`.
 */
double sign_(double a, double b);


#endif //PROYECTOTALLER_SIGN__H
